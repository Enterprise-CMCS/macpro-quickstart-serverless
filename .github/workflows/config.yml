name: Deploy

on:
  push:
    branches:
      - gha*

jobs:
  deploy:
    runs-on: ubuntu-latest

    strategy:
      max-parallel: 0

    container:
      image:  circleci/node:12

    steps:
    - uses: actions/checkout@v1

    - run: env
      working-directory: .
